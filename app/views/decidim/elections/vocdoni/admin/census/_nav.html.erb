<%#
  The census screens share the same tab navigation as every other editor
  screen. This partial exists so census views can keep saying
  `render partial: "nav"` without needing to know the full path.
%>
<%= render "decidim/elections/vocdoni/admin/elections/tabs_menu" %>
