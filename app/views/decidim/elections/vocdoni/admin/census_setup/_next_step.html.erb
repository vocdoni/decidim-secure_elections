<%#
  What happens after this page, in one line — the same strip the Security tab
  ends with, so the wizard reads as one sequence.

  Locals: `chosen`.
%>
<% scope = "decidim.elections.vocdoni.admin.census_setup.next_step" %>
<div class="vocdoni-summary vocdoni-census__next" id="js-census-next-step">
  <span class="vocdoni-summary__title">
    <%= icon "shield-keyhole-line" %>
    <span><%= t("title", scope:) %></span>
  </span>
  <p class="vocdoni-summary__status">
    <span class="vocdoni-summary__sentence"><%= t("body", scope:) %></span>
  </p>
</div>
